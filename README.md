# bsiscioly-web

The tryout page moved to https://tryouts.bsiscioly.org, served by the tryout server's Worker.
This repository only forwards old links there. Generated by `scripts/publish-web.mjs --moved`.
